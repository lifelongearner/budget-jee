import React from "react";
import ReactDOM from "react-dom/client";
import WealthRoadmap from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WealthRoadmap />
  </React.StrictMode>
);
